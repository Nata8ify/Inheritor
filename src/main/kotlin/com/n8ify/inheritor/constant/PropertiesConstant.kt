package com.n8ify.inheritor.constant

object PropertiesConstant {

    /* "pom.xml" generated value's properties */
    const val PROFILES_ACTIVE = "\${spring.profiles.active}"
    const val BUILD_NAME = "\${bulid.name}"
    const val BUILD_VERSION = "\${bulid.version}"
    const val BUILD_TIMESTAMP = "\${bulid.timestamp}"

    /* Request description properties */
    const val REQUEST_DESCRIPTION_ID_PREFIX = "\${requestDesc.idPrefix}"

}